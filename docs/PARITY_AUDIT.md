# Product Parity Audit

Last audited: 2026-07-15. Audit input: `git log --oneline` from the sibling
`bir-python` checkout between the previous anchor `d8f6ced Add sample_rules
for per-trace-name sampling overrides` and the current HEAD `0644048 Release
version 0.3.0` (35 commits; v0.3.0 released 2026-07-03), plus the sibling
`CHANGELOG.md` 0.3.0 section. The product server pins
`bir-sdk>=0.3.0,<0.4.0` in `apps/server/pyproject.toml`.

This checklist maps recent SDK capability areas to the product repo surface.
It is intentionally capability-based rather than a duplicate commit log.

| SDK capability area | Server accepts the event shape | Dashboard displays or summarizes it | Fixtures synced where shared | Docs mention the correct workflow | Product action |
| --- | --- | --- | --- | --- | --- |
| Core trace schema and metadata: `@observe(metadata=...)`, context-manager `set_metadata()`, `generation.set_model()`, nullable `value`/`model`, usage/cost/currency, trace source and service metadata, `rendered_error` prompt-metadata key (v0.3.0: prompt render failures are recorded instead of raising) | Yes. `TraceEventPayload` accepts schema `1.0` (unchanged by v0.3.0), required trace/span/generation/tool/score fields, nullable `value`/`model`, optional `usage`/`cost`/`currency`, JSON metadata/input/output, and extra JSON-compatible fields. It validates parent/root shape and score values. `rendered_error` lives inside the free-form prompt metadata payload, which the server already accepts and redacts. | Yes. Trace detail renders metadata, generations, prompts, retrievals, score groups, service/environment, token totals, cost totals, and model/provider/integration summaries. The trace contract now parses `rendered_error` from prompt metadata and the prompt detail panel surfaces it as a "Render error" payload alongside `rendered`. | Yes. `event-schema-v1.json` and `valid-events.jsonl` are shared fixtures and are byte-for-byte identical to the sibling SDK checkout. Web contract tests cover the `rendered_error` parse path. | Yes. Root `README.md`, `docs/README.md`, and `apps/server/README.md` describe the external SDK boundary, SDK connection flow, local data mode, event contract, cost handling, and filters. Prompt metadata stays free-form in the documented event contract, so `rendered_error` needs no separate doc entry. | No new action. The `rendered_error` display support has landed; sampling and runtime metadata APIs only change which events/metadata the SDK writes. |
| Provider wrappers, async/streaming support, OpenAI Responses, Bedrock, Vertex AI, Mistral, Cohere, LiteLLM, Anthropic, Google, Ollama (new in v0.3.0), async streaming completion for Bedrock/Vertex/Mistral/Cohere/LiteLLM | Yes. These integrations emit ordinary generation/span/tool events with the same model/usage/cost/metadata fields. The Ollama wrappers record model, output text, and `prompt_eval_count`/`eval_count` token usage through the same generic shape. The server does not need provider-specific schemas. | Yes. Provider summaries prefer `metadata.provider` and otherwise derive a dotted provider prefix when no framework integration is set; model, token, and cost summaries are generic and cover Ollama-emitted generations unchanged. | Shared event fixtures cover representative generation shape. Product-only `tests/product-fixtures/integration-events.jsonl` now includes an Ollama generation trace locking its summary behavior. | Yes. Docs state provider wrappers and async/streaming wrappers are SDK-owned, and the `docs/README.md` integration lists include the Ollama provider wrappers (`trace_ollama_chat` / `trace_ollama_generate` and async counterparts), noted as distinct from the playground's Ollama model-server role. | No new action. Provider wrapper logic belongs in `bir-python`; product behavior remains generic event ingestion and display. |
| Framework integrations: Haystack, CrewAI, DSPy, Pydantic AI, Instructor, OpenAI Agents, AutoGen (AG2, new in v0.3.0 via `BirAutoGenHandler` runtime logging) | Yes. Product fixtures exercise these as normal trace/span/generation/tool events with `metadata.integration`, model, usage, cost, and nested parents. AutoGen runs arrive as the same generic contract (trace root, agent-turn spans, generations with usage/cost, tool calls, error status). | Yes. The dashboard has an integration breakdown separate from provider attribution and detail views keep the nested timeline intact; the AutoGen fixture trace exercises both. | Shared fixtures unchanged. Product-only `tests/product-fixtures/integration-events.jsonl` now locks a representative AutoGen multi-agent trace (five events) alongside the existing framework traces. | Yes. Docs list framework integrations as SDK-owned and describe product-side browsing/summarization; the `docs/README.md` integration lists (overview and "SDK Integrations" section) now include AutoGen (AG2) via `BirAutoGenHandler`. | No new action. Product has no framework adapters; it only handles the emitted event contract. |
| Experiments: async runner, `max_workers`, partial `raise_on_error`, per-evaluator tolerances, missing scores, per-example `timeout` (new in v0.3.0), `similarity_above` evaluator, per-example comparison detail (`example_deltas`), `bir experiment-show` | Yes. `/v1/experiments` validates uploaded summaries/results, and `BIR_DATA_DIR` local mode reads SDK-written `.bir/experiments/*.summary.json` plus sibling `.jsonl` rows without rewriting SDK-owned files. A timed-out example is persisted as an ordinary `"error"`-status result row (same shape as any other failure), so no new validation is needed; the SDK-side fix that closes a timed-out trace root also means uploads no longer contain orphaned child events. It tolerates torn final result lines in local mode. | Yes. Experiment detail preserves SDK row order, links `trace_id` rows to trace detail, filters failures (which now include timeout rows), and compares baseline/candidate scores with missing score values. `similarity_above` scores arrive as ordinary evaluator score values; `example_deltas` is SDK CLI/reporting output and never persisted. | Yes. `valid-experiment.json` is a shared fixture and is byte-for-byte identical to the sibling SDK checkout (unchanged by v0.3.0 — timeout reuses the existing error fields and `schema_version` stays `1.0`). Product-only concurrent and partial experiment fixtures lock local-mode behavior. | Yes. Root/server docs describe upload mode, read-only local experiment browsing, concurrent row ordering, partial failed runs, and SDK CLI ownership. | No new action. Timeout rows reuse the error-row shape the product already validates, displays, and filters; evaluators, tolerances, comparison detail, and CLI rendering are SDK-owned. |
| Redaction: configurable extra rules plus built-in Stripe, Azure, PEM private-key, and credit-card/PAN cases (PAN with Luhn validation added in SDK v0.3.0 — cross-repo contract) | Yes. Server redacts metadata, input, output, errors, extra fields, and experiment payloads during validation before write or response normalization. `apps/server/app/redaction.py` independently implements the same PAN rule as the SDK: 13-19 digit runs, optionally split by single spaces or hyphens, replaced only when they pass the Luhn checksum, with 20+ digit runs excluded — mirroring the SDK's `_sdk.py` rule. | Yes. Dashboard displays the redacted values it receives and never reintroduces hidden secrets. | Yes. `redaction-cases.json` — including the v0.3.0 PAN/Luhn cases — is a shared fixture and is byte-for-byte identical to the sibling SDK checkout, so the cross-repo contract flagged in the SDK changelog is satisfied on both sides. | Yes. Docs describe best-effort server redaction and the shared redaction parity fixture. Both repos now also ship a root `SECURITY.md` documenting the redaction posture, the shared cross-repo contract, and a private disclosure process. | No new action. The PAN/Luhn expansion is mirrored and fixture-locked; keep honoring the changelog's cross-repo contract for any future built-in rule change. |
| Sampling, test/log context helpers, and runtime capture controls: `configure(sample_rules=...)`, `bir.testing.capture_traces`, `bir.logging`, trace/span id accessors, master kill switch (`configure(enabled=False)` / `BIR_DISABLED`, new in v0.3.0), capture-size limits (`max_value_length` / `max_collection_items`, new in v0.3.0) | The resulting traces use the same event schema when a sampled/captured trace is written. The kill switch and capture-size limits are SDK runtime controls: they gate whether events are written and how large a captured value can be (truncation runs after redaction), and add no product API fields. Logging filters and id accessors do not add product API fields. | Existing trace display works for captured traces because they are normal SDK events. Truncated values arrive as ordinary strings with a visible `…[truncated]` marker; a disabled SDK simply sends nothing. Logging output is outside the dashboard contract. | No shared fixture change needed beyond existing event fixtures (`schema_version` stays `1.0` for all of these). | Yes. The docs snapshot mentions `sample_rules`, `capture_traces`, `bir.logging`, id accessors, the `enabled`/`BIR_DISABLED` kill switch, and the `max_value_length`/`max_collection_items` capture-size limits as SDK-owned surfaces. | No product action. These features affect SDK runtime behavior or application logs, not the product wire contract. |
| SDK CLIs, OTLP export, package metadata, docs deployment: `bir show`, `bir stats` (now with `--name`/`--status`/`--since`/`--until` filters), `bir traces` filters, `bir config`, `bir prune`, `bir experiment-show`, `bir experiment-report`, `bir export-otel` (richer v0.3.0 export: `deployment.environment`, `bir.source`, `gen_ai.system`), `bir send --mark-sent/--retries/--backoff/--timeout`, `python -m bir`, typed classifier, wheel/sdist release gate, Python 3.14 + cross-platform CI, generated API reference and Pages deploy | Not applicable to ingestion except that CLI-visible local artifacts are the same artifacts local mode can read. Local mode reads exactly `traces.jsonl`, so the `--mark-sent` sidecar (`traces.jsonl.sent`) is ignored and `bir prune`'s atomic whole-trace rewrites are safe for the read-only reader. OTLP export — including the richer resource/span attributes — bypasses the product server by design. | Dashboard is the product counterpart for browsing traces and experiments; it does not implement SDK CLI, report-rendering, or OTLP behavior. | No fixture change required for CLI/packaging/docs-only commits. | Yes. Root, docs, and server READMEs were updated for the 0.3.0 CLI surface: they list `bir config`, `bir prune`, `bir experiment-report`, `bir export-otel`, the `bir traces`/`bir stats` filters, and `python -m bir` as SDK-owned, and note that `bir prune` rewrites SDK-owned files atomically while local mode stays read-only. | No product action. These are SDK packaging, docs, CLI, or exporter surfaces; the docs mentions have already landed. |
| Shared fixture drift guard | Not an event shape by itself, but it protects the schema, valid trace, redaction cases, and experiment upload contract used by server tests. | Web tests read the shared fixtures directly for trace and experiment normalization. | Yes. Product and sibling SDK shared fixtures plus `CHECKSUMS.sha256` — including the PAN-expanded `redaction-cases.json` — are byte-for-byte identical in this audit. | Yes. `tests/fixtures/README.md` documents canonical ownership, `sync`, and `check`. | No product action. Continue using `python scripts/fixtures.py check`; use `sync` for paired fixture changes. |

## Verification Checklist

- Fixture drift guard: `python scripts/fixtures.py check`
- Server tests: run the server pytest suite from `apps/server`
- Pyright: run if `pyright` is installed locally
- Web tests: `npm test` from `apps/web`
- Web typecheck: `npm run typecheck` from `apps/web`
- Web lint: `npm run lint` from `apps/web`

Results for this audit (2026-07-15): fixture check OK (4 shared fixtures
match `CHECKSUMS.sha256`), server pytest 170 passed, web tests 142 passed,
typecheck clean, lint clean. Pyright was not installed locally and was
skipped per the checklist.
