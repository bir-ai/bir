# Product Parity Audit

Last audited: 2026-06-27. Audit input: `git log --oneline -n 40` from the
sibling `bir-python` checkout, headed by `d8f6ced Add sample_rules for
per-trace-name sampling overrides`.

This checklist maps recent SDK capability areas to the product repo surface.
It is intentionally capability-based rather than a duplicate commit log.

| SDK capability area | Server accepts the event shape | Dashboard displays or summarizes it | Fixtures synced where shared | Docs mention the correct workflow | Product action |
| --- | --- | --- | --- | --- | --- |
| Core trace schema and metadata: `@observe(metadata=...)`, context-manager `set_metadata()`, `generation.set_model()`, nullable `value`/`model`, usage/cost/currency, trace source and service metadata | Yes. `TraceEventPayload` accepts schema `1.0`, required trace/span/generation/tool/score fields, nullable `value`/`model`, optional `usage`/`cost`/`currency`, JSON metadata/input/output, and extra JSON-compatible fields. It validates parent/root shape and score values. | Yes. Trace detail renders metadata, generations, prompts, retrievals, score groups, service/environment, token totals, cost totals, and model/provider/integration summaries. Filters cover source, service, environment, status, name, event type, duration, and sort. | Yes. `event-schema-v1.json` and `valid-events.jsonl` are shared fixtures and are byte-for-byte identical to the sibling SDK checkout. | Yes. Root `README.md`, `docs/README.md`, and `apps/server/README.md` describe the external SDK boundary, SDK connection flow, local data mode, event contract, cost handling, and filters. | No new action. Product already consumes the generic event shape; sampling and runtime metadata APIs only change which events/metadata the SDK writes. |
| Provider wrappers, async/streaming support, OpenAI Responses, Bedrock, Vertex AI, Mistral, Cohere, LiteLLM, Anthropic, Google | Yes. These integrations emit ordinary generation/span/tool events with the same model/usage/cost/metadata fields. The server does not need provider-specific schemas. | Yes. Provider summaries prefer `metadata.provider` and otherwise derive a dotted provider prefix when no framework integration is set; model, token, and cost summaries are generic. | Shared event fixtures cover representative generation shape. Product-only integration fixtures cover richer provider/integration summary behavior. | Yes. Docs state provider wrappers and async/streaming wrappers are SDK-owned and the product stores, queries, and displays emitted events. | No new action. Provider wrapper logic belongs in `bir-python`; product behavior remains generic event ingestion and display. |
| Framework integrations: Haystack, CrewAI, DSPy, Pydantic AI, Instructor, OpenAI Agents | Yes. Product fixtures exercise these as normal trace/span/generation/tool events with `metadata.integration`, model, usage, cost, and nested parents. | Yes. The dashboard has an integration breakdown separate from provider attribution and detail views keep the nested timeline intact. | Shared fixtures unchanged. Product-only `tests/product-fixtures/integration-events.jsonl` locks the representative integration traces. | Yes. Docs list framework integrations as SDK-owned and describe product-side browsing/summarization. | No new action. Product has no framework adapters; it only handles the emitted event contract. |
| Experiments: async runner, `max_workers`, partial `raise_on_error`, per-evaluator tolerances, missing scores, `bir experiment-show` | Yes. `/v1/experiments` validates uploaded summaries/results, and `BIR_DATA_DIR` local mode reads SDK-written `.bir/experiments/*.summary.json` plus sibling `.jsonl` rows without rewriting SDK-owned files. It tolerates torn final result lines in local mode. | Yes. Experiment detail preserves SDK row order, links `trace_id` rows to trace detail, filters failures, and compares baseline/candidate scores with missing score values. | Yes. `valid-experiment.json` is a shared fixture and is byte-for-byte identical to the sibling SDK checkout. Product-only concurrent and partial experiment fixtures lock local-mode behavior. | Yes. Root/server docs describe upload mode, read-only local experiment browsing, concurrent row ordering, partial failed runs, and SDK CLI ownership. | No new action. SDK execution, CLI rendering, tolerances, and row production are SDK-owned; product handles persisted artifacts and display. |
| Redaction: configurable extra rules plus built-in Stripe, Azure, PEM private-key cases | Yes. Server redacts metadata, input, output, errors, extra fields, and experiment payloads during validation before write or response normalization. | Yes. Dashboard displays the redacted values it receives and never reintroduces hidden secrets. | Yes. `redaction-cases.json` is a shared fixture and is byte-for-byte identical to the sibling SDK checkout. | Yes. Docs describe best-effort server redaction and the shared redaction parity fixture. | No new action. Product-side redaction is independently implemented and fixture-locked against the SDK cases. |
| Sampling and test/log context helpers: `configure(sample_rules=...)`, `bir.testing.capture_traces`, `bir.logging`, trace/span id accessors | The resulting traces use the same event schema when a sampled/captured trace is written. Logging filters and id accessors do not add product API fields. | Existing trace display works for captured traces because they are normal SDK events. Logging output is outside the dashboard contract. | No shared fixture change needed beyond existing event fixtures. | Yes. Docs snapshot mentions `sample_rules`, `capture_traces`, `bir.logging`, and id accessors as SDK-owned surfaces. | No product action. These features affect SDK runtime behavior or application logs, not the product wire contract. |
| SDK CLIs, OTLP export, package metadata, docs deployment: `bir show`, `bir stats`, `bir experiment-show`, `bir export-otel`, `python -m bir`, typed classifier, wheel extras, SDK docs site | Not applicable to ingestion except that CLI-visible local artifacts are the same artifacts local mode can read. OTLP export bypasses the product server by design. | Dashboard is the product counterpart for browsing traces and experiments; it does not implement SDK CLI or OTLP behavior. | No fixture change required for packaging/docs-only commits. | Yes. Root/server docs explicitly state CLI commands and OTLP export are SDK-owned, and local mode reads the same `.bir` artifacts in read-only mode. | No product action. These are SDK packaging, docs, CLI, or exporter surfaces. |
| Shared fixture drift guard | Not an event shape by itself, but it protects the schema, valid trace, redaction cases, and experiment upload contract used by server tests. | Web tests read the shared fixtures directly for trace and experiment normalization. | Yes. Product and sibling SDK shared fixtures plus `CHECKSUMS.sha256` are byte-for-byte identical in this audit. | Yes. `tests/fixtures/README.md` documents canonical ownership, `sync`, and `check`. | No product action. Continue using `python scripts/fixtures.py check`; use `sync` for paired fixture changes. |

## Verification Checklist

- Fixture drift guard: `python scripts/fixtures.py check`
- Server tests: run the server pytest suite from `apps/server`
- Pyright: run if `pyright` is installed locally
- Web tests: `npm test` from `apps/web`
- Web typecheck: `npm run typecheck` from `apps/web`
- Web lint: `npm run lint` from `apps/web`

